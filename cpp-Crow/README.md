# Crow flavor

## Requirements

Build Crow `build-framework.sh`, adapter `../c-exprtk-adapter`, Google Test `../test/build-googletest.sh` first

## Build

```shell
./build.sh
```

## Run

```shell
./build/calc
```
