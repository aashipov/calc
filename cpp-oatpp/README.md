# Oat++ flavor

## Build

```shell
rm -rf build && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && make && cd ..
```

## Run

```shell
./build/calc-oatpp
```

Check [SwaggerUI](http://0.0.0.0:8080/swagger/ui)
