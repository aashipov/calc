# ZIO Http (Scala) Flavor

Even weirder 'meaningless semantics excess' than `../ktor` (Kotlin)

## Requirements

Build `../c-exprtk-adapter` first

## Build & run

```shell
mvn clean package
```

or

```shell
./build.sh
```
