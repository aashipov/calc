# Dart flavor #

## Requirements

Build `../c-exprtk-adapter` first

## Build & run

```shell
dart test && dart run
```

or

```shell
dart test && dart build cli && ./build/cli/linux_x64/bundle/bin/calc
```